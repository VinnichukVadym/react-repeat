const RouterEndpoints = {
    index: '/',
    users: 'users',
    posts: 'posts',
    comments: 'comments',
    userId: ':userId',
    postId: ':postId',
    commentsId: ':commentsId',
}

export {
    RouterEndpoints,
}